# Messenger Module - Технічна документація

## 📋 Огляд

Модуль месенджера для додатку MyReactNative з підтримкою повідомлень в реальному часі.

## 🏗️ Структура бази даних

### Таблиці

#### 1️⃣ **Conversations** (Розмови)
Зберігає інформацію про чати (приватні та групові).

| Поле | Тип | Опис |
|------|-----|------|
| Id | int | Первинний ключ |
| Name | string? | Назва групового чату |
| IsGroup | bool | Чи є груповим чатом |
| GroupPhotoPath | string? | Шлях до аватару групи |
| CreatedById | int | ID створювача |
| CreatedAt | DateTime | Дата створення |
| LastMessageAt | DateTime? | Час останнього повідомлення |

#### 2️⃣ **Messages** (Повідомлення)
Зберігає всі повідомлення в розмовах.

| Поле | Тип | Опис |
|------|-----|------|
| Id | int | Первинний ключ |
| ConversationId | int | ID розмови |
| SenderId | int | ID відправника |
| Content | string? | Текст повідомлення |
| MessageType | string | text, image, file, audio, video |
| FilePath | string? | Шлях до медіа файлу |
| FileName | string? | Назва файлу |
| FileSize | long? | Розмір файлу в байтах |
| ReplyToMessageId | int? | ID повідомлення для відповіді |
| IsEdited | bool | Чи відредаговане |
| IsDeleted | bool | Чи видалене |
| CreatedAt | DateTime | Час створення |
| EditedAt | DateTime? | Час редагування |

#### 3️⃣ **ConversationParticipants** (Учасники розмов)
Зв'язує користувачів з розмовами.

| Поле | Тип | Опис |
|------|-----|------|
| Id | int | Первинний ключ |
| ConversationId | int | ID розмови |
| UserId | int | ID користувача |
| Role | string | admin/member |
| JoinedAt | DateTime | Час приєднання |
| LeftAt | DateTime? | Час виходу |
| IsActive | bool | Чи активний |
| IsMuted | bool | Чи вимкнені сповіщення |

#### 4️⃣ **MessageReadStatuses** (Статуси прочитання)
Відстежує, хто прочитав повідомлення.

| Поле | Тип | Опис |
|------|-----|------|
| Id | int | Первинний ключ |
| MessageId | int | ID повідомлення |
| UserId | int | ID користувача |
| ReadAt | DateTime | Час прочитання |
| IsDelivered | bool | Чи доставлене |

## 🚀 Технологія Real-time: SignalR

### Чому SignalR?

**SignalR** - це бібліотека від Microsoft для додавання функціональності real-time до веб-додатків.

#### ✅ Переваги SignalR:

1. **Простота інтеграції** з ASP.NET Core
2. **Автоматичний вибір транспорту**:
   - WebSockets (найшвидший)
   - Server-Sent Events
   - Long Polling (fallback)
3. **Підтримка груп** - ідеально для чатів
4. **Automatic reconnection** - відновлення з'єднання
5. **Масштабованість** через Redis/Azure SignalR Service
6. **TypeScript підтримка** для React Native

#### 📊 Альтернативи (не обрані):

| Технологія | Чому НЕ обрана |
|------------|----------------|
| Socket.io | Потребує Node.js backend |
| WebSockets (чистий) | Більше коду, немає fallback |
| Firebase Realtime DB | Vendor lock-in, додаткові витрати |
| Pusher/Ably | Платні сервіси |
| gRPC Streaming | Складніша реалізація для веба |

### Архітектура SignalR

```
┌─────────────────┐         WebSocket/SSE         ┌──────────────────┐
│  React Native   │ ◄──────────────────────────► │   ChatHub        │
│     Client      │                               │  (SignalR Hub)   │
└─────────────────┘                               └──────────────────┘
                                                           │
                                                           ▼
                                                  ┌──────────────────┐
                                                  │   ApplicationDb  │
                                                  │     Context      │
                                                  └──────────────────┘
```

## 🔧 SignalR Hub - Методи

### ChatHub методи:

#### 📤 **Для клієнта → сервер:**

- `JoinConversation(conversationId)` - Приєднатися до групи чату
- `LeaveConversation(conversationId)` - Покинути групу  
- `SendMessage(conversationId, senderId, content, type)` - Відправити повідомлення
- `UserTyping(conversationId, userId, userName)` - Користувач набирає
- `UserStoppedTyping(conversationId, userId)` - Користувач зупинився
- `MarkMessageAsRead(messageId, userId)` - Позначити як прочитане
- `EditMessage(messageId, newContent)` - Редагувати повідомлення
- `DeleteMessage(messageId)` - Видалити повідомлення

#### 📥 **Для сервер → клієнт:**

- `ReceiveMessage` - Отримати нове повідомлення
- `UserTyping` - Хтось набирає
- `UserStoppedTyping` - Хтось зупинився
- `MessageRead` - Повідомлення прочитано
- `MessageEdited` - Повідомлення відредаговано
- `MessageDeleted` - Повідомлення видалено

## 📡 REST API Endpoints

### Conversations API:

```
POST   /api/messenger/conversations          - Створити розмову
GET    /api/messenger/conversations/{userId} - Отримати розмови користувача
GET    /api/messenger/conversations/{id}/messages?page=1&pageSize=50 - Повідомлення
POST   /api/messenger/conversations/{id}/read?userId={userId} - Позначити як прочитане
```

## 🔐 Безпека (TODO)

На наступних етапах потрібно додати:

1. **JWT Authentication** - аутентифікація користувачів
2. **Authorization** - перевірка доступу до розмов
3. **Message Encryption** - шифрування повідомлень (E2E опціонально)
4. **Rate Limiting** - захист від спаму
5. **Content validation** - валідація контенту

## 📱 Клієнт (React Native)

### Встановлення SignalR клієнта:

```bash
npm install @microsoft/signalr
```

### Приклад підключення:

```typescript
import * as signalR from "@microsoft/signalr";

const connection = new signalR.HubConnectionBuilder()
  .withUrl("http://YOUR_API_URL/chathub")
  .withAutomaticReconnect()
  .build();

// Підключення
await connection.start();

// Приєднатися до чату
await connection.invoke("JoinConversation", conversationId);

// Слухати нові повідомлення
connection.on("ReceiveMessage", (message) => {
  console.log("New message:", message);
});

// Відправити повідомлення
await connection.invoke("SendMessage", conversationId, userId, "Hello!", "text");
```

## 🎯 Індекси для оптимізації

База даних містить такі індекси для швидкої роботи:

- `IX_Conversations_CreatedById` - пошук розмов за створювачем
- `IX_Conversations_LastMessageAt` - сортування за останнім повідомленням
- `IX_ConversationParticipant_ConversationId_UserId` (UNIQUE) - учасники
- `IX_Messages_ConversationId_CreatedAt` - повідомлення в розмові
- `IX_MessageReadStatus_MessageId_UserId` (UNIQUE) - статуси прочитання

## 🚀 Масштабування (майбутнє)

### Для великої кількості користувачів:

1. **Redis Backplane** - для синхронізації між серверами
   ```csharp
   builder.Services.AddSignalR()
       .AddStackExchangeRedis("REDIS_CONNECTION_STRING");
   ```

2. **Azure SignalR Service** - managed-сервіс від Microsoft

3. **Database sharding** - розподіл даних по базах

4. **CDN для медіа** - зберігання файлів окремо

## 📝 Приклади використання

### Створення приватної розмови:

```http
POST /api/messenger/conversations
Content-Type: application/json

{
  "createdById": 1,
  "participantIds": [2],
  "isGroup": false
}
```

### Створення групового чату:

```http
POST /api/messenger/conversations
Content-Type: application/json

{
  "createdById": 1,
  "participantIds": [2, 3, 4],
  "groupName": "Team Chat",
  "isGroup": true
}
```

### Отримання історії повідомлень:

```http
GET /api/messenger/conversations/5/messages?page=1&pageSize=50
```

## 🎨 Особливості реалізації

1. **Soft delete** - повідомлення не видаляються фізично (IsDeleted = true)
2. **Message editing** - зберігається час редагування
3. **Reply support** - можливість відповідати на повідомлення
4. **Read receipts** - підтвердження прочитання
5. **Typing indicators** - індикатори набору тексту
6. **Unread counters** - лічильники непрочитаних

## 📚 Наступні кроки

- [ ] Додати аутентифікацію JWT
- [ ] Реалізувати завантаження медіа файлів
- [ ] Додати пуш-нотифікації
- [ ] Створити React Native компоненти
- [ ] Додати тести
- [ ] Оптимізувати запити до БД
- [ ] Додати логування подій
